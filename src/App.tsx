import { useState } from "react";
import "./App.css";

interface Country {
  name: {
    common: string;
    official: string;
  };
  flags: {
    svg: string;
  };
  cca3: string; // Usaremos el código de 3 letras para la navegación
  region: string;
}

function App() {
  return <></>;
}

export default App;
